import { ReactNode } from "react";

export const metadata = {
  title: "BAH Bible | Access Pending",
  description: "Born of Fire, an internal training and operations manual for BAH employees."
};

export default function AccessPendingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}