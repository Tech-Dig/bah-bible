import { ReactNode } from "react";

export const metadata = {
  title: "BAH Bible | Login",
  description: "Born of Fire, an internal training and operations manual for BAH employees."
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}